/*learning to write closures well 
the main work of the outer function is to retunr the inner function  and the work of the inner function is
to use the variables from both scopes*/
const createGreater = (greeting) => {
     console.log('Hello')

     const usingGreater = (name) => {
      console.log()  
     }
}
